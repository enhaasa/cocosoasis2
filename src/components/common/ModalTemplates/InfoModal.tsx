import { useState } from 'react';

type Props = {
    image?: string;
    content: React.ReactNode;
}

function InfoModal({ image, content }:Props) {



    return (
        <div className="InfoModal">
            
        </div>
    )
}

export default InfoModal;